#include <stdio.h>

/**
 * this program is to identify syntactic errors in C
 *
 * Return: 0.
 */
int main (void)
{
	int sum; /* declare variable */

	/* COMPUTE RESULT */
	sum = 25 + 37 - 19;
	/* DISPLAY RESULTS */
	printf ("The answer is %i\n", sum);

	return (0);
}
