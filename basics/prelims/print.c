#include <stdio.h>

/**
 * This program prints out some statements 
 *
 * Return: 0.
 */
int main (void)
{
	/* calling printf function to print statements */
	printf("In C, lowercase letters are significant\n"
			"main is where program execution begins.\n"
			"Opening and closing braces enclose program"
			" statements in a routine.\n"
			"All program statements must be terminated"
			" by a semicolon.\n");

	return (0);
}
