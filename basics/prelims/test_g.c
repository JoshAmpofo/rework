#include <stdio.h>

/**
 * testing out %g
 */
int main (void)
{
	float num = 331.79;

	printf("%g\n", num); /* %g prints a more nicer version of floats (leaves out any trailing 0s) */

	return (0);
}
